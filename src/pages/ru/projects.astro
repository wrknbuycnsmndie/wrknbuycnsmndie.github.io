---
import { getCollection } from 'astro:content';
import ProjectsPage from '../../views/ProjectsPage.astro';

const projects = (await getCollection('projectsRu')).sort(
	(a, b) => a.data.createdAt.getTime() - b.data.createdAt.getTime(),
);
---

<ProjectsPage projects={projects} locale="ru" />
