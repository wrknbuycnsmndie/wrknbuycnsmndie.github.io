<section class="home-hero">
	<div class="hero-content">
		<h1>Hey! Nice to meet you!</h1>
		<p class="intro">I'm developer, that making systems, do you need one?</p>
		<p class="intro">
			This is my personal space for selected projects, experiments, and notes about the things I am
			working on.
		</p>
	</div>
</section>

<style>
	.home-hero {
		animation: enter var(--duration-slow) var(--ease-out) both;
	}

	.hero-content {
		max-width: 50rem;
	}

	.intro {
		max-width: 35rem;
		margin: var(--space-6) 0 0;
		color: var(--color-text-muted);
		font-size: var(--font-size-lg);
	}

	@keyframes enter {
		from {
			opacity: 0;
			transform: translateY(1.125rem);
		}

		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.home-hero {
			animation: none;
		}
	}
</style>
